{
    "version": "0.2.0",
    "compounds": [
        {
            "name": "RUN_ALL",
            "configurations": [
                "BACK"
            ],
            "preLaunchTask": "build",
            "stopAll": true
        }
    ],
    "configurations": [
        {
            "name": "BACK",
            "type": "coreclr",
            "request": "launch",
            "requireExactSource": false,
            "cwd": "${workspaceFolder}/Back",
            "program": "${workspaceFolder}/Back/bin/Debug/net10.0/Back.dll",
        }
    ]
}
